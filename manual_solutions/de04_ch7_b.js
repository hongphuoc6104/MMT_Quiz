window.MANUAL_SOLUTIONS=Object.assign(window.MANUAL_SOLUTIONS||{}, {
"De04-11-267":{knowledge:"UDP checksum for IPv4 is computed over a pseudo-header plus UDP header/data. IPv4 pseudo-header includes source IP, destination IP, zero byte, protocol number (17), and UDP length. It is not actually transmitted as part of UDP header; it protects against misdelivery.",reasoning:"Among provided choices, D is closest because it includes source IP, destination IP and UDP length, but it still omits the protocol field (and reserved zero field). Source warning correctly marks it incomplete.",options:{a:{why:"Incomplete: source IP alone is only one pseudo-header field.",when:"Correct only if question asks whether source IP participates in pseudo-header checksum."},b:{why:"Incomplete: UDP length is included but not the addresses/protocol.",when:"Correct if asking whether UDP length participates."},c:{why:"Closer but still incomplete: both IP addresses are included, yet length and protocol also matter.",when:"Correct if asking only which address fields are present."},d:{why:"Best available answer: includes both IP addresses and UDP length. It is still not a complete pseudo-header description because protocol (17) and the zero/reserved field are omitted.",when:"Correct as the nearest option in this question; for a complete definition add protocol and reserved zero."}},commonMistakes:["Thinking pseudo-header is transmitted on the wire as UDP header fields.","Forgetting protocol number is part of IPv4 UDP pseudo-header."],summary:"UDP pseudo-header: src IP + dst IP + zero + protocol + UDP length; D is nearest but incomplete."},
"De04-12-268":{knowledge:"Transport-layer multiplexing/demultiplexing identifies application endpoints with port numbers. IP identifies host/interface at layer 3; a socket endpoint combines IP+port, and TCP connections are identified by a 4-tuple.",reasoning:"B=port number.",options:{a:{why:"IP distinguishes hosts/interfaces, not individual processes on one host.",when:"Correct if asking how network layer identifies destination host/interface."},b:{why:"Correct. TCP/UDP destination port selects the receiving socket/process service.",when:"Correct when asking process differentiation at transport layer."},c:{why:"Hostname is resolved by DNS and is not the transport demultiplexing key.",when:"Correct if asking human-friendly naming of hosts/services before resolution."},d:{why:"OS process names are local implementation labels, not fields carried by TCP/UDP.",when:"Correct only in local OS administration, not network transport demux."}},commonMistakes:["Answering IP address and forgetting many processes share one host IP."],summary:"Transport distinguishes application endpoints primarily by port numbers."},
"De04-13-269":{knowledge:"TCP is byte-stream oriented: sequence numbers count bytes in the stream, not application messages or TCP segment numbers. Message boundaries from application writes are not preserved as protocol records.",reasoning:"C=Byte.",options:{a:{why:"TCP serves applications but its sequence space is not 'application-oriented'.",when:"Correct if asking which layer consumes TCP service: applications/processes."},b:{why:"Physical transmission is bits, but TCP service abstraction is bytes.",when:"Correct if asking physical-layer data unit."},c:{why:"Correct. TCP provides an ordered byte stream.",when:"Correct when describing TCP service/sequence numbering."},d:{why:"'User-oriented' is not a TCP data orientation term.",when:"Could describe human-facing application context, not TCP stream semantics."}},commonMistakes:["Thinking TCP preserves message/segment boundaries; it exposes a byte stream."],summary:"TCP is byte-oriented; sequence numbers refer to bytes."},
"De04-14-270":{knowledge:"If a TCP segment starts with sequence number S and carries n data bytes, those bytes occupy sequence numbers S through S+n−1. The next expected byte is therefore S+n, so cumulative ACK=S+n (ignoring SYN/FIN special consumption when not part of this data segment).",reasoning:"B gives Acknowledgement=SequenceNum+n.",options:{a:{why:"Adds one twice. If n bytes start at S, last byte is S+n−1, so next byte is S+n, not S+n+1.",when:"Could be correct if an additional SYN/FIN also consumed one sequence number beyond the n data bytes."},b:{why:"Correct for an ordinary data segment carrying n bytes beginning at SequenceNum.",when:"Correct when no extra SYN/FIN sequence-space consumption is included."},c:{why:"S+n−1 is the sequence number of the last received byte, not the next expected byte advertised in ACK.",when:"Correct if question asks sequence number of final byte in the segment."},d:{why:"S+1 only works when exactly one byte (or SYN-only semantics) is being acknowledged.",when:"Correct for n=1 data byte, or ACK of a SYN with no data under TCP sequence rules."}},calculation:{title:"TCP cumulative ACK",steps:["First byte sequence=S.","n bytes occupy S,…,S+n−1.","ACK reports next expected byte.","Therefore ACK=S+n."],result:"Acknowledgement = SequenceNum + n."},commonMistakes:["Returning last received byte number instead of next expected byte."],summary:"TCP ACK points to next expected byte: ACK=S+n."},
"De04-15-271":{knowledge:"TCP URG flag indicates that the Urgent Pointer field is significant under TCP urgent-data semantics. SYN establishes sequence synchronization, PSH asks for prompt delivery semantics, FIN closes a direction.",reasoning:"C=URG.",options:{a:{why:"SYN is used during connection establishment, not to mark urgent data.",when:"Correct when synchronizing initial sequence numbers in handshake."},b:{why:"PSH relates to push semantics, not the Urgent Pointer.",when:"Correct when application wants received data promptly delivered through the stream interface."},c:{why:"Correct. URG marks the Urgent Pointer as significant.",when:"Correct for TCP urgent-data mechanism."},d:{why:"FIN indicates sender has no more stream data and half-closes that direction.",when:"Correct when terminating a TCP direction gracefully."}},commonMistakes:["Confusing PSH with URG because both sound like 'send immediately'."],summary:"Urgent data semantics use URG; PSH is different."},
"De04-16-272":{knowledge:"A TCP SEQ value in a diagram must be derived from the preceding initial sequence number and the number of bytes/SYN/FIN consumed in that same direction. The repository does not contain the original page-65 diagram asset for this question, so the numeric source key cannot be independently reconstructed from the surviving text alone.",reasoning:"The source answer is 2048. Because the diagram referenced by 'Hình đâu' is absent from repo assets, this manual solution does not invent missing byte counts; it records the key and the method needed to verify it once the original figure is recovered.",options:{a:{why:"This is the source-key answer: SEQ=2048. It is plausible only if the previous sequence state/data lengths in the missing diagram advance the sender to byte 2048.",when:"Correct if the recovered figure shows next sender sequence number 2048 after accounting for bytes/SYN/FIN."},b:{why:"4096 cannot be justified from the text without the diagram.",when:"Would be correct if preceding transmitted data advanced the same direction's sequence space to 4096."},c:{why:"0 cannot be justified; TCP ISNs are not generally fixed at zero.",when:"Could be correct only in a simplified diagram that explicitly initializes sequence to 0."},d:{why:"2047 would be the last byte before 2048 in a contiguous byte stream, not necessarily the next segment's starting SEQ.",when:"Correct if the diagram asks for last byte sequence rather than next starting sequence."}},calculation:{title:"Cách kiểm tra khi phục hồi hình",steps:["Theo dõi riêng chiều của bên gửi.","Lấy SEQ bắt đầu trước đó.","Cộng số byte dữ liệu đã gửi; cộng 1 cho SYN/FIN nếu có.","Giá trị thu được là SEQ của segment kế tiếp."],result:"Khóa nguồn: 2048; hiện không thể tái tính vì ảnh gốc trang 65 không có trong repo."},commonMistakes:["Bịa phép tính từ các đáp án khi hình đã mất.","Cộng sequence của hai chiều TCP vào nhau."],summary:"Source key=2048, nhưng cần ảnh gốc để kiểm chứng phép tính; không suy diễn dữ kiện thiếu."},
"De04-17-273":{knowledge:"TCP Sequence Number field for a segment containing data gives the sequence number of the first data byte in that segment. TCP sequence numbers count byte positions in the stream, not segment ordinal numbers.",reasoning:"D describes the first byte's sequence number.",options:{a:{why:"Wrong. TCP does not simply number segments 1,2,3; segment sizes vary and sequence space counts bytes.",when:"A separate protocol could number packets/segments explicitly, but not TCP Sequence Number semantics."},b:{why:"Wrong. The last byte is SEQ+length−1, not the value stored as SEQ for the segment.",when:"Correct if question asks how to calculate sequence number of the final data byte."},c:{why:"Wrong. Service port addresses are Source Port/Destination Port fields, not Sequence Number.",when:"Correct if asking which fields identify transport service endpoints."},d:{why:"Correct. SEQ identifies first byte in the segment's payload stream (with SYN semantics considered separately).",when:"Correct for TCP data segments."}},commonMistakes:["Treating TCP SEQ as a segment counter instead of byte position."],summary:"TCP SEQ = sequence number of first byte in the segment."},
"De04-18-274":{knowledge:"UDP is the connectionless transport protocol in the TCP/IP suite. TCP is connection-oriented. ICMP and RARP/ARP are not transport protocols.",reasoning:"B=UDP.",options:{a:{why:"ICMP supports IP control/error messaging, not transport process-to-process service.",when:"Correct if asking protocol used by ping/error control at Internet layer."},b:{why:"Correct. UDP sends independent datagrams without connection setup.",when:"Correct when asking connectionless transport."},c:{why:"RARP is a legacy address-resolution protocol, not transport.",when:"Correct if asking historical MAC→IP address resolution."},d:{why:"TCP is connection-oriented and stateful.",when:"Correct if asking reliable connection-oriented transport."}},commonMistakes:["Choosing ICMP because it is also connectionless; the question specifically asks transport layer."],summary:"Connectionless transport=UDP."},
"De04-19-275":{knowledge:"TCP is the connection-oriented transport protocol: endpoints establish state, sequence numbers and flow-control context before normal data transfer. UDP is connectionless.",reasoning:"B=TCP.",options:{a:{why:"UDP does not perform connection establishment.",when:"Correct if asking connectionless transport."},b:{why:"Correct. TCP is connection-oriented.",when:"Correct when asking transport protocol with connection setup/reliable stream."},c:{why:"RARP is not transport.",when:"Correct only for legacy address-resolution question."},d:{why:"ICMP is Internet-layer control, not transport.",when:"Correct if asking error/echo control protocol."}},commonMistakes:["Equating 'stateful application' over UDP with UDP itself being connection-oriented."],summary:"Connection-oriented transport=TCP."},
"De04-20-276":{knowledge:"TCP flow control uses a sliding receive window advertised in acknowledgements; sender limits outstanding bytes accordingly. TCP also has congestion control (cwnd), so actual send allowance is constrained by both flow and congestion state.",reasoning:"D=Cửa sổ trượt.",options:{a:{why:"Stop-and-Wait would permit only one outstanding unit and waste TCP's byte-stream pipelining capacity.",when:"Correct for simple ARQ protocols with window size1."},b:{why:"Byte streaming is a service abstraction, not the mechanism that controls sender throughput against receiver buffer.",when:"Correct if asking TCP data orientation."},c:{why:"False. TCP explicitly performs flow control using advertised window, in addition to congestion control.",when:"Only an unreliable protocol without receiver flow control might fit this statement, not TCP."},d:{why:"Correct. TCP's advertised/sliding window controls how many bytes may be outstanding.",when:"Correct when asking TCP flow-control method."}},commonMistakes:["Confusing flow control (receiver capacity/rwnd) with congestion control (network capacity/cwnd)."],summary:"TCP flow control uses a sliding advertised receive window."}
});